import { Select2Directive } from './select2.directive';

describe('Select2Directive', () => {
  it('should create an instance', () => {
    const directive = new Select2Directive();
    expect(directive).toBeTruthy();
  });
});
